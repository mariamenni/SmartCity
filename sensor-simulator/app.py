# Ce fichier n'est plus utilise par le conteneur Docker.
#
# Le Dockerfile clone directement arnauropero/smart-cities-api (FastAPI, port 8000).
# Endpoints disponibles :
#   GET /api/v1/sensors
#   GET /api/v1/readings/{sensor_id}?limit=100
#   GET /api/v1/metrics
#   GET /health
#
# Ce fichier est conserve uniquement comme reference historique du prototype Flask.
