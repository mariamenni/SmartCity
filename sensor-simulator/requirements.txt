# Dependances de la smart-cities-api (arnauropero/smart-cities-api)
# Ces packages sont aussi dans requirements.txt du repo clone,
# mais listés ici pour reference et compatibilité locale.
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
